events { worker_connections 1024; }
http {
    upstream myapp {
        server app:3000;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://myapp;
        }
    }
}